/// <reference types="cypress" />

import { API } from "cypress/support/API"

context("Login to SAP web gui", () => {

    it("Login", () => {
        // cy.visit('url')

        // cy.get('cssElement')
        
        // cy.get('cssElement').click()
        // cy.log('log', var)
        // cy.get('cssElement').should('be.visible')
        
    })

})
